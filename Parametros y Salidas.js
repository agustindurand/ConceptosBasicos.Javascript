/* La funcion puede comportarse como una operacion que genera valores, en el espacio donde se llama a la funcion se genera un nuevo valor, e
este valor es el definido por el return de la misma */ 

/* VA A RETORNAR UN VALOR */ 

 function sumar (primerNumero, segundoNumero) { 
    return primerNumero + segundoNumero; }
  let resultado = sumar (5,8);
// Javascript provee el objeto Math que funciona como un contendor de herramientas y metodos para realizar operaciones maticas complejas //

// Ver todo lo que se puede hacer en libreria MDN //
// es un objeto incorporado que posee propiedades //

console.log( Math.E); //2,718..//

console.log (Math.PI); // 3,14159...//

// tambien posee METODOS, entre ellos un metodo para buscar maximos y minimos entre una serie de numeros por ejemplo //

console.log (Math.max (55, 13, 12, 1)); 
console.log (Math.min(55, 14, 13, 12, 66));
console.log (Math.max (22, Infinity, 22, 12, 14));//Toma el valor Infinity como el mas alto // 

// Ejemplo Practico con Arrays de Min y Max // 

const array = [12, 13, 14, 15];
console.log (Math.min (12, 13, 14, 15)); //Colocamos valores a mano//
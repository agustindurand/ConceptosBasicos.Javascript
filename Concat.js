

// Combinamos dos arrays distintos en uno solo resultante // 
// no afecta a los array originales //

const perritos = ["Firulais", "Pipo", "Pachu"];
const gatitos = ["Mumi", "Pepe"]; 

const mascotas = gatitos.concat (perritos);
console.log (mascotas); 